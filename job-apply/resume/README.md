# Resumes (private)

Do **not** commit Montanna's resume DOCX or contact details to this public repo.

- Enhanced resume is attached on the private Notion hub page.
- Keep local copies outside git (e.g. `~/Documents/` or Cursor uploads).
- Cover-letter drafts can be regenerated with:

```bash
python3 job-apply/scripts/make_cover_letters.py
```

Add `job-apply/output/cover_letters/` and any `*.docx` resumes to `.gitignore`.
